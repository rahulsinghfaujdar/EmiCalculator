import { ImageSourcePropType } from 'react-native';

const arrowBack: ImageSourcePropType = require('./arrow_back.png');
const visibility: ImageSourcePropType = require('./visibility.png');
const visibilityOff: ImageSourcePropType = require('./visibility_off.png');

export const icons = {
  arrowBack,
  visibility,
  visibilityOff,
};

export { 
    arrowBack, 
    visibility, 
    visibilityOff 
};

export default icons;
